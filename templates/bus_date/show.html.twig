{% extends 'base.html.twig' %}

{% block title %}BusDate{% endblock %}

{% block body %}
    <h1>BusDate</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ bus_date.id }}</td>
            </tr>
            <tr>
                <th>PlaceRestantes</th>
                <td>{{ bus_date.placeRestantes }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_bus_date_index') }}">back to list</a>

    <a href="{{ path('app_bus_date_edit', {'id': bus_date.id}) }}">edit</a>

    {{ include('bus_date/_delete_form.html.twig') }}
{% endblock %}
