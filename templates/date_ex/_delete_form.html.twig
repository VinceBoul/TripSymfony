<form method="post" action="{{ path('app_date_ex_delete', {'id': date_ex.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ date_ex.id) }}">
    <button class="btn">Delete</button>
</form>
